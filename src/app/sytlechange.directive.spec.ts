import { SytlechangeDirective } from './sytlechange.directive';

describe('SytlechangeDirective', () => {
  it('should create an instance', () => {
    const directive = new SytlechangeDirective();
    expect(directive).toBeTruthy();
  });
});
